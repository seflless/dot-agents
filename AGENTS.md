# About me

Full Name: Francois Laberge
Birthday: March 5th, 1979
Occupation: Software Engineer by training (Currently CEO/Solo-founder of Decode)

# Speaking style

- I'm very much anti-formality, but do like to be professional when it makes sense (like using computer science terms when that's more clear to use)
- I most often use Whsprflow to dictate what I'm "writing" to you. So take what I say with a grain of salt and ask when something seems like it might be a transcription issue, unless you are confident you can interpret it without any risk. Speed over 100% safety is my preference.

## Responses to me

Always recap very briefly how I can verify/review/test work that you have finished on my behalf? Unless it seems like I already know, I'm juggling a lot of coding agent chats. So even though you've said something up in our conversation, I may have forgot or it's too much work to scroll up each time.

# About Decode

Decode is a company making a whiteboard and browser for coding agents. It let's your visually communicate with coding agents to build software and includes a browser to view and debug the apps that are being built. It was started approximately February 15th, 2025 full time. Where I raised $1M in pre-seed. We're working towards a follow on round now.

# When using plan mode

- Make the plan extremely concise. Sacrifice grammar for the sake of concision.
- At the end of each plan, give me a list of unresolved questions to answer, if any.

# gstack

Use the `/browse` skill from gstack for all web browsing. Never use `mcp__claude-in-chrome__*` tools.

Available skills: `/office-hours`, `/plan-ceo-review`, `/plan-eng-review`, `/plan-design-review`, `/design-consultation`, `/review`, `/ship`, `/land-and-deploy`, `/canary`, `/benchmark`, `/browse`, `/qa`, `/qa-only`, `/design-review`, `/setup-browser-cookies`, `/setup-deploy`, `/retro`, `/investigate`, `/document-release`, `/codex`, `/cso`, `/autoplan`, `/careful`, `/freeze`, `/guard`, `/unfreeze`, `/gstack-upgrade`.
